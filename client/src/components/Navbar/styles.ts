import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  appBar: {
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: '15px',
    marginBottom: '1rem',
  },
});
